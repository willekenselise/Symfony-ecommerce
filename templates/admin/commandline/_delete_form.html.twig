<form method="post" action="{{ path('app_admincommandline_delete', {'id': commandline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandline.id) }}">
    <button class="btn">Delete</button>
</form>
