{% extends 'base.html.twig' %}

{% block title %}Commandline{% endblock %}

{% block body %}
    <h1>Commandline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandline.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ commandline.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admincommandline_index') }}">back to list</a>

    <a href="{{ path('app_admincommandline_edit', {'id': commandline.id}) }}">edit</a>

    {{ include('admin/commandline/_delete_form.html.twig') }}
{% endblock %}
